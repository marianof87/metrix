# metrix — variables de entorno (ejemplo)
# Copie este archivo a .env y ajuste los valores. NO suba secretos reales.

# SQLite vía Prisma — ruta del archivo de base de datos
DATABASE_URL="file:./dev.db"
